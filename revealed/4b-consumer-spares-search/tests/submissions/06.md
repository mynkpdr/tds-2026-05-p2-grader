Negative available quantities show inventory corruption, so the global spare data cannot be trusted. Fix all negative stock before testing semantic search.

Evidence table
| claim | source | confidence |
| Two rows have negative available quantity | spare_parts.csv | high |

Rejected: operational explanation; inventory cannot be negative.
