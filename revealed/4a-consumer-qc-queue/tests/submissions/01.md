The dashboard should not use the export's elapsed time yet. `qcore_release_ts` is suspiciously concentrated at one clock time, so I would first verify what that timestamp represents and whether it is event time or interface time.

Evidence table
| claim | source | confidence |
|---|---|---|
| Release times are mechanically clustered rather than distributed like human work | data/qc/batch_release.csv (`qcore_release_ts` hour profile) | high |
| Cross-system comparisons may not be point-in-time aligned | data/platform/source_freshness.csv | high |
| The SOP's 24h target starts when required evidence is available, not simply at lab completion | data/process/qc_release_sop.md | high |

Rejected: “QC team only releases overnight.” Possible, but less likely because the same minute repeats across sites; I would not reject it fully without the event log. Rejected: “holiday backlog explains the whole pattern.” A holiday can explain a local spike, not the repeated clock-time concentration.

Next request: one week of LabTrack/QCore status-event history with both user action time and interface commit time, plus the data-catalog definition of `qcore_release_ts`.
